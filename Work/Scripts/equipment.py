class Equipment:

    def __init__(self, new_id, title, description, number, reserve_num):
        self.__id = new_id
        self.__title = title
        self.__description = description
        self.__number = number
        self.__reserve_num = reserve_num

